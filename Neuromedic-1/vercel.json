{
  "buildCommand": "cd neuromedic && npm install && npm run build",
  "outputDirectory": "neuromedic/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}